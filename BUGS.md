- Change name from "Circuit Map" - visible on Map page as well as when downloaded on iOS device. Name should be "MMAF". "Circuit Map" text should be deleted from the map. If someone wants to sponsor the map later we can put their logo there.

- Logo on IOS should be made from "MMAF_Primary_CMYK.svg" which is already square-ish, not part of the horizontal logo.

- The browser tab icon shows a gray cube next to "Midway Music & Arts Festival", not sure where that came from. Is that the favicon? That should be just the stylized M from Midway in the logo image.

- The icons on the map should be about 50% bigger. They look OK on desktop but they're too small on mobile.

- On the map on desktop, the letters and number on the map icons aren't centered within the diamonds.

- The map is at a good default zoom level, but you should be able to zoom out a couple more notches so you can see more context about where you are. It should include at least the raymond ave. green line station, which needs to be added to the transit list. The map should be centered on Hamline Park (venue 6). The primary event area will be within 1 mile north and south of the park and 2 miles east or west of it. The map should be scrollable to show an additional 1 mile of context in each direction for a total of 4 miles north/south by 6 miles east/west dimensions.

- The map legend does not need to be labeled "Legend", that text can be deleted.

- When you click on a transit station on the map, it should render a details view similar to clicking on a venue or sponsor. The stations exist as locations on Google Maps, so there should be a bit of info about the transit station and a link or links to open it in google maps (or your default maps app - is that an option?)

- On schedule page, remove filtering by category/kind. Grouping by kind should stay.

- When scrolling the schedule page, the background content is visible in a tiny slice between the bottom of the header and the top of the filters section, which is distracting. The header logo section should scroll out of the way the same way the "Schedule" header line does as you scroll down the page, and the filters should stay visible. This should work the same on all scrollable pages.

- Add 21+ or 18+ age limit labels to the event list. An event might have an age limit. This will be a new column on the events sheet under header age_limit and the values will be either blank, "18+" or "21+". A value of 18+ or 21+ should show the appropriate icon on the event.

- The current ticket icon doesn't look very good. I've put two SVG icons for free and paid tickets in the MMAF Brand Assets directory. These SVGs are black/white, but they should be red/white or red/transparent on the site to match the color scheme.

- The instructions to install this app at the bottom for Safari should include the share button icon where it says "tap the share button". The share instructions are also inaccurate for Safari on Mac, where you must choose "Add to Dock".

- On the Starred page, The text should not refer to iOS if the user is not on an iOS device. Maybe just make it more generic, e.g. "They may be cleared by the browser if the site is unused for about a week." Maybe add the "install" prompt here as well, explaining that installing the app prevents the storage from being cleared.

- We don't like the way the star icon looks inside the box. Need to come up with some alternatives. What's good about it is that it is on the right hand side which makes it convenient for tapping with your right thumb if you're right handed on a mobile device. We're thinking maybe make the star a bit larger and remove the box around it, but interested to hear some ideas about this. I think having the star inside the main box would make it too crowded and make it harder to distinguish between clicking on the star and clicking on the box.

- Multiple labels on an event should stack vertically rather than horizontally. So for example the ticket required icon should be under the Music label rather than next to it. Same with the new age limit labels.

- When un-starring an item on the starred page, it disappears immediately, which makes it a bit annoying to find again if you unstar something by accident. I was thinking maybe they could stay around for a few seconds, maybe grayed out or dimmed, before disappearing, to give you a chance to undo before they are gone from the page.

- I'm wondering if the transit icons should include other local bus stops in the area, particularly from the 67 and 72 bus lines. However, it's a large number of stops and might clutter the map.
