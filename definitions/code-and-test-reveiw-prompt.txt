You are leading an in-depth code and test review of the Midway Music & Arts Fest site — a small, offline-capable map/schedule PWA at /Users/amanfredi/midway-music-and-art-festival. The festival is Oct 2–4, 2026, about 8 weeks out. The codebase grew through fast QA rounds and no one has read it end to end since. Anthony, the repo owner, will triage your findings into follow-up work and use your evidence for two pending decisions: whether to adopt MapLibre GL JS for the map, and whether to adopt a UI/PWA library that would absorb accessibility and cross-browser concerns the code currently hand-rolls.

The binding specification is definitions/code-and-test-review.md. Read it in full first, along with CLAUDE.md, DEFINITION.md, CONTRACTS.md, and BACKLOG.md. Where this prompt and the definition conflict, the definition wins.

You orchestrate: do the setup yourself, dispatch three parallel lens agents (fresh contexts keep each read sharp; synthesis stays with you), then merge, research, verify, and write the report.

Stage 1 — setup (you, before any dispatch):
- Derive the key user-facing feature list from DEFINITION.md.
- Capture the screenshot baseline with tools/shoot.mjs: all six routes × phone/desktop/narrow viewports × key interactive states (open detail sheet, map zoom levels, install prompt). Commit the PNGs plus a recipe file with exact reproduction commands to reviews/2026-08-baseline/, plain message ending [skip ci].
- Run the empirical checks yourself, serialized, so no two builds ever run concurrently: npm run build twice and diff the generated outputs between runs (determinism); npm test (its initial build fetches the live venues sheet — that network access is expected); one fetch-failure simulation (build with the sheet URL unreachable) recording actual behavior. Keep exact outputs. Then restore regenerated tracked paths (git checkout -- site/sw.js site/data site/assets/sponsors); if a fresh build differs from the committed outputs, that is itself a finding (live-sheet drift or nondeterminism), not a mess to hide.

Stage 2 — dispatch three lens agents in parallel (Agent tool, model opus, one message). Give each: the repo path, the feature list, the definition path, the empirical outputs relevant to it, the finding format below, and these rules — quote the offending lines before analyzing (no quote, no finding); precision over recall, a false positive wastes trust; before returning, re-verify every finding against the source and drop any that don't hold; strictly read-only — run no builds or tests, change no files.

- App agent — site/ (~3,200 lines of JS, CSS, HTML). Three jobs. (a) Reuse: copy/pasted patterns across views versus shared components, quoting the duplicated instances. (b) Hand-rolled surface inventory: routing, store/state, sheet/dialog semantics, focus management, ARIA, keyboard handling, gestures/pan/zoom, label collision — for each: approximate line count, defects or BACKLOG.md items traceable to it, accessibility sensitivity, and whether a mature library plausibly absorbs it. (c) Render-time sinks: where content.json fields and OSM-derived SVG reach the DOM, and the escaping at each sink — venue and event data originates in a live Google Sheet.
- Test agent — tests/ plus coverage assessment. Philosophy (the generic parts of /Users/amanfredi/code/claude-config/plugins/tf/skills/test-review/REFERENCE.md apply; skip PATTERNS.md, it's RSpec-specific): every test has a cost and earns it only by catching bugs that would otherwise ship; "no test needed" is a first-class answer; flag gaps only where a bug has real festival-goer consequences. Map coverage feature-by-feature against the provided list; assess the existing suite's quality — what it would and wouldn't catch, fixture realism versus the live sheet, flake risk; propose missing tests as business-invariant checks, not implementation mirrors.
- Tooling agent — scripts/ and tools/ (~2,200 lines), correctness and integrity only, no style review. (a) Paths by which a broken site could be published: partial builds, bad data passing validation, CI/deploy gaps. (b) Untrusted data: trace the Google Sheet CSV and OSM/Overpass responses through every transformation into build outputs, checking validation and escaping at each step. (c) Availability: what happens to the ability to publish when the sheet or Overpass is unreachable — what's cached, what's fatal. Interpret the empirical outputs you're given (determinism diff, fetch-failure behavior).

Finding format (all agents): slug; file:line; severity — P1 user-facing breakage or integrity risk, P2 meaningful cost or gap, P3 worth noting; confidence verified|inferred; quoted evidence; failure scenario or ongoing cost; one-line resolution direction (fixes are follow-ups, not this review's work).

Stage 3 — synthesis (you):
- Merge and dedup findings. Reconcile the seams — in particular, end-to-end escaping of sheet data spans the tooling and app agents' territory; confirm the handoff got covered and cover it yourself if not.
- Spot-check every P1 and P2 against the source before accepting it.
- Desk-research candidates, keyed to the app agent's inventory: MapLibre GL JS, at least two UI-layer candidates you select and justify, optionally a service-worker library such as Workbox. Per candidate: gzipped bundled size, build-time bundling and offline feasibility, WebGL/Lockdown-Mode and low-end-device risk for the map, documented accessibility track record, and what fraction of the inventory it would actually replace. Source, version, and confidence label on every claim; label stale or unverifiable numbers rather than smoothing them over.
- Write reviews/2026-08-code-and-test-review.md: prioritized findings in user-impact order → hand-rolled surface inventory → candidate assessment addressed to the two decisions → proposed follow-up list (fixes, spikes including on-device iOS-lag profiling, WCAG-audit sequencing) formatted so items lift into BACKLOG.md. Commit it [skip ci]. Do not push.

Constraints (you and every agent): Change no code — this is report-only; the only files created are the baseline directory and the report. Reason: review-plus-rewrite muddies the deliverable and risks regressions two months before the festival. No WCAG 2.2 conformance audit — separate backlog item; the a11y inventory is its input, not its replacement. No spikes — no device profiling, no prototype builds; where reading can't settle a question (notably the cause of the reported iOS scroll/zoom lag) say so plainly and define the follow-up spike rather than presenting inference as diagnosis. Don't decide the library questions — present trade-offs and a labeled recommendation; the decisions are Anthony's. The working tree has pre-existing uncommitted changes to the top-level docs: commit only files you created, never stage anything else.

Verification before claiming done: the report contains the actual determinism-diff and test-suite outputs, pass or fail. Every finding retains file:line, evidence, severity, and confidence. Re-read the finished report against the definition's success criteria; its named failure modes — generic advice, unverified claims in the voice of fact, findings that require re-reading the code to understand — are grounds to revise before finishing.

Boundaries: an actively exploitable injection reachable through a live data source goes at the top of the report, and your final message leads with it. If npm test fails before you've changed anything, that's a finding, not something to fix. Out of scope entirely: editing site or tooling code, editing the definition or BACKLOG.md, pushing, deploying.

Final message: one-paragraph summary; the top five findings; the report path; anything the lens agents disagreed on and how you resolved it.
